# Copy the same AI_GATEWAY_API_KEY used by the root app's chat configuration.
# VERCEL_OIDC_TOKEN is an optional fallback (local tokens expire and may need refreshing).
AI_GATEWAY_API_KEY=""
VERCEL_OIDC_TOKEN=""
READING_AGENT_SECRET=""